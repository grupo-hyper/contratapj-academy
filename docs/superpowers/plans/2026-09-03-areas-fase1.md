# Áreas — Fase 1 (Organizar) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Introduzir a entidade **Área** acima dos módulos: migrar o conteúdo atual para a área "Comercial", transformar a Home num **Hub de Áreas**, servir a trilha por área em `/area/:slug`, e escopar o CMS por área. Tudo público, edição central (admin/autor). Sem RLS por área ainda (F2/F3).

**Architecture:** Nova tabela `areas` + `modules.area_id`. Front: `useAreas` (lista de áreas) → `AreaHubPage` (`/`); a Home-trilha atual vira `AreaTrilhaPage` (`/area/:slug`) filtrando por `area_id`. CMS ganha gestão de áreas + árvore escopada por área. Padrões existentes: react-query + Supabase, Vitest + Testing Library, TDD, commits frequentes. RLS de admin (`is_admin()`, 0008) já cobre escrita para admins.

**Tech Stack:** React 18 + TS + Vite + Tailwind v4 (cores `cpj`), @tanstack/react-query, Supabase, react-router-dom, Vitest.

**Regras do projeto:** `main` é pushada pelo Diego; migrations **coladas no chat** e aplicadas por ele no SQL Editor; responder em PT-BR; "feito" só após deploy.

---

## File Structure

- `supabase/migrations/0009_areas.sql` (novo) — schema de áreas + `modules.area_id` + backfill Comercial.
- `src/types/content.ts` (modificar) — tipo `Area` + `area_id` em `Module`.
- `src/features/areas/useAreas.ts` (novo) — lista de áreas visíveis (F1: todas publicadas).
- `src/features/areas/AreaHubPage.tsx` (novo) — grid de cards das áreas (rota `/`).
- `src/features/areas/AreaCard.tsx` (novo) — card de uma área (capa + nome + descrição).
- `src/features/home/useHomeData.ts` (modificar) — aceitar `areaId` e filtrar módulos por área.
- `src/features/areas/AreaTrilhaPage.tsx` (novo) — resolve slug→área, renderiza a trilha (reusa a UI da Home atual) escopada.
- `src/features/home/HomePage.tsx` (modificar) — extrair a UI da trilha para ser reusada com `areaId`.
- `src/router.tsx` (modificar) — `/` → `AreaHubPage`; `/area/:slug` → `AreaTrilhaPage`.
- `src/features/authoring/useAreaAdmin.ts` (novo) — CRUD de áreas (admin).
- `src/features/authoring/AreaManager.tsx` (novo) — UI de gestão de áreas no CMS.
- `src/features/authoring/useAuthorTree.ts` (modificar) — aceitar `areaId`, filtrar módulos da área.
- `src/features/authoring/useAuthorMutations.ts` (modificar) — `createModule(areaId)`.
- `src/features/authoring/AuthorPage.tsx` (modificar) — seletor de área + passar `areaId` à árvore/criação.

---

## Task 0: Migration `0009_areas.sql` (SQL aplicado pelo Diego)

**Files:** Create `supabase/migrations/0009_areas.sql`.

Não é TDD (SQL). Colar o SQL no chat; Diego roda um **preview** e depois aplica no SQL Editor. Commit do arquivo no repo.

- [ ] **Step 1: Escrever a migration** (idempotente onde possível):

```sql
-- 0009_areas.sql — entidade Área acima dos módulos (Fase 1: organizar)
-- 1) Tabela areas
create table if not exists public.areas (
  id           uuid primary key default gen_random_uuid(),
  nome         text not null check (length(btrim(nome)) > 0),
  slug         text not null unique,
  descricao    text,
  capa_url     text,
  visibilidade text not null default 'publica' check (visibilidade in ('publica','restrita')),
  ordem        int  not null,
  publicado    boolean not null default true,
  created_at   timestamptz not null default now()
);
alter table public.areas enable row level security;
-- F1: leitura de áreas publicadas p/ todos; escrita só admin (RLS por área vem na F2/F3).
drop policy if exists areas_select on public.areas;
create policy areas_select on public.areas for select to authenticated
  using (publicado = true or public.is_admin());
drop policy if exists areas_admin_all on public.areas;
create policy areas_admin_all on public.areas for all to authenticated
  using (public.is_admin()) with check (public.is_admin());
grant select, insert, update, delete on public.areas to authenticated;

-- 2) Área "Comercial" (só cria se ainda não houver nenhuma área)
insert into public.areas (nome, slug, descricao, visibilidade, ordem, publicado)
select 'Comercial', 'comercial', 'Playbooks do time Comercial (Contrata PJ).', 'publica', 1, true
where not exists (select 1 from public.areas);

-- 3) modules.area_id + backfill + constraints por área
alter table public.modules add column if not exists area_id uuid references public.areas (id) on delete cascade;
update public.modules set area_id = (select id from public.areas where slug = 'comercial')
  where area_id is null;
alter table public.modules alter column area_id set not null;
-- troca a unicidade/globalidade de ordem para por-área
alter table public.modules drop constraint if exists modules_ordem_key;      -- unique(ordem) auto-nome
alter table public.modules drop constraint if exists modules_ordem_check;    -- check(1..12) auto-nome
-- (se os nomes acima diferirem, ver: select conname from pg_constraint where conrelid='public.modules'::regclass;)
alter table public.modules add constraint modules_area_ordem_key unique (area_id, ordem);
alter table public.modules add constraint modules_ordem_pos_check check (ordem >= 1);
create index if not exists idx_modules_area_id on public.modules (area_id);
```

- [ ] **Step 2: Preview p/ o Diego conferir antes de aplicar:**

```sql
-- nomes reais das constraints de ordem (para ajustar os DROPs se preciso)
select conname, pg_get_constraintdef(oid) from pg_constraint
where conrelid = 'public.modules'::regclass and conname ~ 'ordem';
-- após aplicar: todos os módulos devem apontar p/ Comercial
select a.slug, count(m.id) from public.modules m join public.areas a on a.id=m.area_id group by a.slug;
```

- [ ] **Step 3: Commit do arquivo** (o efeito vem quando Diego aplica):

```bash
git add supabase/migrations/0009_areas.sql
git commit -m "feat(db): migration de areas + modules.area_id (Fase 1)"
```

**Nota:** os testes de front mockam o supabase, então não dependem da migration aplicada para rodar verdes. Mas o app real só funciona após o Diego aplicar.

---

## Task 1: Tipo `Area` + `area_id` no `Module`

**Files:** Modify `src/types/content.ts`.

- [ ] **Step 1:** Adicionar a interface `Area` e o campo `area_id` em `Module`:

```ts
export interface Area {
  id: string
  nome: string
  slug: string
  descricao: string | null
  capa_url: string | null
  visibilidade: 'publica' | 'restrita'
  ordem: number
  publicado: boolean
  created_at: string
}
// Em Module: adicionar
//   area_id: string
```

- [ ] **Step 2:** `npx tsc -b --noEmit` → exit 0 (só adição de tipo).
- [ ] **Step 3:** Commit: `git commit -am "feat(types): Area + Module.area_id"`.

---

## Task 2: `useAreas` — lista de áreas visíveis

**Files:** Create `src/features/areas/useAreas.ts` + `useAreas.test.tsx`.

Responsabilidade: buscar as áreas (F1: `select * from areas order by ordem`) via react-query. Degradar PGRST205 (tabela ausente) → `[]` (padrão do projeto, ver `useGoals`).

- [ ] **Step 1: Teste (TDD)** — mock do supabase (`from('areas').select().order()` resolve com 2 áreas); asserta `areas` ordenadas e `isLoading/isError`. Espelhar o harness de `useManagerClasses.test.tsx`.
- [ ] **Step 2:** Rodar → falha (hook não existe).
- [ ] **Step 3: Implementar** `useAreas()`: query `['areas']`, `fetchAreas()` = `supabase.from('areas').select('*').order('ordem', { ascending: true })`, com o guard PGRST205 → `[]`. Retorna `{ areas: Area[], isLoading, isError, error }`.
- [ ] **Step 4:** Rodar → passa.
- [ ] **Step 5:** Commit `feat(areas): useAreas lista as áreas visíveis`.

---

## Task 3: `AreaCard` — card de uma área

**Files:** Create `src/features/areas/AreaCard.tsx` + `AreaCard.test.tsx`.

Presentational: recebe `area: Area`, renderiza um `<Link to={/area/${area.slug}}>` com capa (fallback sem imagem), nome e descrição. Estilo dark coerente com `Tile.tsx`/`Hero.tsx` (cores `cpj`, `rounded-2xl`, borda `cpj-white/10`).

- [ ] **Step 1: Teste** — renderiza nome + link com `href` `/area/<slug>`; sem capa mostra fallback (ex.: inicial do nome). Usar `MemoryRouter`.
- [ ] **Step 2:** Falha.
- [ ] **Step 3:** Implementar o card.
- [ ] **Step 4:** Passa.
- [ ] **Step 5:** Commit `feat(areas): AreaCard`.

---

## Task 4: `AreaHubPage` — grid de áreas (rota `/`)

**Files:** Create `src/features/areas/AreaHubPage.tsx` + `AreaHubPage.test.tsx`.

Usa `useAreas()`. Estados: loading (skeleton), erro inline, vazio ("Nenhuma área publicada."), e o grid de `<AreaCard>` (responsivo: `grid gap-6 sm:grid-cols-2 lg:grid-cols-3`). Cabeçalho simples ("Áreas" / marca). Dentro do `<AppLayout>` (sidebar já existe).

- [ ] **Step 1: Teste** — mock `useAreas` (2 áreas) → renderiza 2 cards; mock vazio → mensagem "Nenhuma área publicada".
- [ ] **Step 2:** Falha.
- [ ] **Step 3:** Implementar.
- [ ] **Step 4:** Passa.
- [ ] **Step 5:** Commit `feat(areas): AreaHubPage (hub na Home)`.

---

## Task 5: `useHomeData` aceita `areaId` (trilha escopada)

**Files:** Modify `src/features/home/useHomeData.ts` (+ ajustar `useHomeData.test.tsx`).

Hoje `useHomeData()` busca todos os módulos. Passar a aceitar `useHomeData(areaId?: string)`: quando `areaId` presente, filtrar a query de módulos por `.eq('area_id', areaId)`; a queryKey vira `['modules', areaId]`. Sem `areaId`, comportamento atual (para não quebrar nada durante a transição). As demais queries (lessons, progresso) permanecem; `lessonsByModule` já é derivado dos módulos retornados.

- [ ] **Step 1: Teste** — mock supabase; ao chamar `useHomeData('area-1')`, a query de módulos aplica `.eq('area_id','area-1')` (espionar o `eq`). Ajustar mocks existentes que quebrarem.
- [ ] **Step 2:** Rodar os testes de home → o novo falha.
- [ ] **Step 3: Implementar** o parâmetro + filtro + queryKey por área.
- [ ] **Step 4:** Rodar → todos passam (inclusive os antigos, retrocompatíveis).
- [ ] **Step 5:** Commit `feat(home): useHomeData escopado por area_id`.

---

## Task 6: `AreaTrilhaPage` — trilha da área (rota `/area/:slug`)

**Files:** Create `src/features/areas/AreaTrilhaPage.tsx` + test. Modify `src/features/home/HomePage.tsx` para extrair a UI da trilha num componente reusável `TrilhaView({ modules, lessonsByModule, ... })` (ou reusar `HomePage` recebendo `areaId`).

Abordagem recomendada (menor churn): `AreaTrilhaPage` lê `:slug` (useParams), usa `useAreas()` para resolver `slug→area`, e então renderiza a **mesma UI da trilha** passando `areaId`. Extrair o corpo visual de `HomePage` para um componente `TrilhaView` consumido por ambas; ou fazer `HomePage` aceitar `areaId` e `AreaTrilhaPage` montar `<HomePage areaId={area.id} />`. Escolher a que menos altera os testes atuais de `HomePage`.

- [ ] **Step 1: Teste** — `AreaTrilhaPage` em `/area/comercial` (mock `useAreas` com Comercial + mock `useHomeData`) renderiza os módulos da área; slug inexistente → "Área não encontrada".
- [ ] **Step 2:** Falha.
- [ ] **Step 3: Implementar** (extração da `TrilhaView` + resolução de slug + estado "não encontrada").
- [ ] **Step 4:** Passa; rodar os testes de `HomePage` para garantir que a extração não quebrou.
- [ ] **Step 5:** Commit `feat(areas): AreaTrilhaPage (/area/:slug)`.

---

## Task 7: Router — `/` = hub, `/area/:slug` = trilha

**Files:** Modify `src/router.tsx` (+ `src/router.test.tsx`).

Trocar `{ path: '/', element: <HomePage /> }` por `{ path: '/', element: <AreaHubPage /> }` e adicionar `{ path: '/area/:slug', element: <AreaTrilhaPage /> }` (ambas dentro do `<AppLayout>`, qualquer papel autenticado). Ajustar `router.test.tsx`: os testes que hoje checam `/nenhum módulo publicado/i` na raiz passam a checar o hub (ex.: `/nenhuma área publicada/i` ou o nome de uma área mockada). Mockar `useAreas` no router.test (padrão dos outros mocks lá).

- [ ] **Step 1:** Atualizar router + adicionar mock `useAreas` no router.test + ajustar as asserções da raiz.
- [ ] **Step 2:** `npx vitest run src/router.test.tsx` → verde.
- [ ] **Step 3:** Commit `feat(router): / vira hub de áreas + /area/:slug`.

---

## Task 8: `useAreaAdmin` — CRUD de áreas (admin)

**Files:** Create `src/features/authoring/useAreaAdmin.ts` + test.

Mutations: `createArea({nome, slug, descricao, capa_url, visibilidade, ordem, publicado})`, `updateArea(id, patch)`, `deleteArea(id)`. Invalida `['areas']`. Espelhar `useManagerClasses`/`useAuthorMutations` (mesma estrutura de useMutation + invalidação + isMutating/isMutationError). RLS: `areas_admin_all` (0009) libera admin.

- [ ] **Step 1: Teste** — mock supabase por operação; asserta insert/update/delete com os args e invalidação de `['areas']`.
- [ ] **Step 2:** Falha.
- [ ] **Step 3: Implementar.**
- [ ] **Step 4:** Passa.
- [ ] **Step 5:** Commit `feat(authoring): useAreaAdmin (CRUD de áreas)`.

---

## Task 9: `AreaManager` — UI de gestão de áreas no CMS

**Files:** Create `src/features/authoring/AreaManager.tsx` + test.

Lista as áreas (via `useAreas`) + form de criar/editar (nome, slug, descrição, capa_url, visibilidade [select publica/restrita], ordem, publicado [toggle]) usando `useAreaAdmin`. Excluir com `window.confirm` (aviso: remove os módulos da área em cascata). Estilo coerente com `ModuleEditor`.

- [ ] **Step 1: Teste** — renderiza áreas; criar chama `createArea`; editar chama `updateArea`; excluir pede confirm e chama `deleteArea`.
- [ ] **Step 2:** Falha.
- [ ] **Step 3: Implementar.**
- [ ] **Step 4:** Passa.
- [ ] **Step 5:** Commit `feat(authoring): AreaManager (gestão de áreas)`.

---

## Task 10: CMS escopado por área

**Files:** Modify `src/features/authoring/useAuthorTree.ts` (+test), `src/features/authoring/useAuthorMutations.ts` (+test), `src/features/authoring/AuthorPage.tsx` (+test).

- `useAuthorTree(areaId?)`: filtrar a query de módulos por `.eq('area_id', areaId)` quando presente; queryKey `['author_tree','modules', areaId]`. Aulas seguem por module_id.
- `useAuthorMutations.createModule(areaId)`: incluir `area_id: areaId` no insert; a "próxima ordem" passa a ser calculada **dentro da área** (`.eq('area_id', areaId)`).
- `AuthorPage`: adicionar um **seletor de área** no topo (usando `useAreas`), guardar `selectedAreaId`, passar à árvore e à criação de módulo. Incluir a aba/entrada para o `AreaManager` (ex.: um item "Gerenciar áreas"). Default: primeira área (ou Comercial).

- [ ] **Step 1: Testes** — `useAuthorTree('area-1')` aplica `.eq('area_id','area-1')`; `createModule('area-1')` insere com `area_id`; `AuthorPage` mostra o seletor e troca a árvore ao mudar de área. Ajustar testes existentes (passam a mockar `useAreas`).
- [ ] **Step 2:** Falham.
- [ ] **Step 3: Implementar.**
- [ ] **Step 4:** `npx vitest run` inteiro → verde.
- [ ] **Step 5:** Commit `feat(authoring): CMS escopado por área`.

---

## Task 11: Verificação final da Fase 1

- [ ] **Step 1:** `npx vitest run` → tudo verde.
- [ ] **Step 2:** `npx tsc -b --noEmit` → exit 0.
- [ ] **Step 3:** `npm run build` → exit 0.
- [ ] **Step 4:** `npm run lint` → sem NOVOS erros.
- [ ] **Step 5:** Colar o SQL da Task 0 no chat para o Diego aplicar; instruir hard-refresh; smoke: Home mostra o hub com "Comercial", clicar abre a trilha, CMS lista/edita áreas e a árvore escopada.

---

## Self-Review (cobertura do spec)

- Áreas (tabela + visibilidade) → Task 0/1. ✅
- Hub na Home → Task 2/3/4/7. ✅
- Trilha por área `/area/:slug` → Task 5/6/7. ✅
- CMS por área + gestão de áreas → Task 8/9/10. ✅
- Migrar Comercial → Task 0 (backfill). ✅
- `area_members`/RLS por área → **NÃO** (é F2/F3, fora do escopo da F1, correto). ✅
- Turmas/metas → fora de escopo (spec). ✅

Sem placeholders; nomes de hooks/props consistentes entre tasks (`useAreas`, `areaId`, `selectedAreaId`, `useAreaAdmin`).
